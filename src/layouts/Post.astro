---
import type { MarkdownHeading } from 'astro';
import Base from './Base.astro';
import PostToc from '../components/PostToc.astro';
import ReadingProgress from '../components/ReadingProgress.tsx';
import AuthorFooter from '../components/AuthorFooter.astro';
import BackToTop from '../components/BackToTop.astro';
import CodeCopyEnhancer from '../components/CodeCopyEnhancer.astro';
import PartOfProject from '../components/PartOfProject.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  pillar?: string;
  tags?: string[];
  image?: string;
  headings: MarkdownHeading[];
  minutes?: number;
  prev?: { slug: string; title: string } | null;
  next?: { slug: string; title: string } | null;
  project?: string;
}

const {
  title,
  description,
  pubDate,
  updatedDate,
  pillar,
  tags,
  image,
  headings,
  minutes,
  prev,
  next,
  project,
} = Astro.props;

const formatDate = (d: Date) =>
  d.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
---

<Base title={title} description={description} image={image} ogType="article">
  <script slot="head" type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": title,
    "description": description,
    "image": image ? new URL(image, "https://coreymark.com").toString() : "https://coreymark.com/og-default.png",
    "datePublished": pubDate instanceof Date ? pubDate.toISOString() : pubDate,
    "dateModified": updatedDate ? (updatedDate instanceof Date ? updatedDate.toISOString() : updatedDate) : (pubDate instanceof Date ? pubDate.toISOString() : pubDate),
    "url": new URL(Astro.url.pathname, "https://coreymark.com").toString(),
    "author": { "@type": "Person", "name": "Corey Wininger", "url": "https://coreymark.com/about" },
    "publisher": { "@type": "Person", "name": "Corey Wininger", "url": "https://coreymark.com" }
  })} />
  <ReadingProgress client:load />

  <article class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:grid lg:grid-cols-[minmax(0,1fr)_16rem] lg:gap-12">
    <div class="min-w-0">
      <header class="mb-10">
        {project && <PartOfProject projectSlug={project} />}
        {pillar && (
          <div class="mb-3 text-xs font-semibold uppercase tracking-wide text-accent-primary">
            {pillar}
          </div>
        )}
        <h1 class="mb-4 text-4xl font-semibold leading-tight tracking-tight md:text-5xl">
          {title}
        </h1>
        <p class="mb-5 text-lg text-text-secondary">{description}</p>
        <div class="flex flex-wrap items-center gap-x-2 text-sm text-text-tertiary">
          <time datetime={pubDate.toISOString()}>{formatDate(pubDate)}</time>
          {minutes && (
            <>
              <span aria-hidden="true">·</span>
              <span>{minutes} min read</span>
            </>
          )}
          {updatedDate && (
            <>
              <span aria-hidden="true">·</span>
              <span>
                Updated{' '}
                <time datetime={updatedDate.toISOString()}>
                  {formatDate(updatedDate)}
                </time>
              </span>
            </>
          )}
          {tags && tags.length > 0 && (
            <>
              <span aria-hidden="true">·</span>
              <ul class="flex flex-wrap gap-2">
                {tags.map((tag) => (
                  <li class="text-text-tertiary">#{tag}</li>
                ))}
              </ul>
            </>
          )}
        </div>
      </header>

      <div class="prose prose-invert">
        <slot />
      </div>

      <AuthorFooter />

      {(prev || next) && (
        <nav
          aria-label="Adjacent posts"
          class="mt-16 grid gap-4 border-t border-border-subtle pt-8 sm:grid-cols-2"
        >
          {prev ? (
            <a
              href={`/blog/${prev.slug}`}
              class="group rounded-lg border border-border-subtle bg-bg-elevated p-4 transition-colors hover:border-accent-primary"
            >
              <div class="text-xs uppercase tracking-wide text-text-tertiary">
                ← Previous
              </div>
              <div class="mt-1 text-text-primary group-hover:text-accent-primary">
                {prev.title}
              </div>
            </a>
          ) : <div />}
          {next ? (
            <a
              href={`/blog/${next.slug}`}
              class="group rounded-lg border border-border-subtle bg-bg-elevated p-4 text-right transition-colors hover:border-accent-primary"
            >
              <div class="text-xs uppercase tracking-wide text-text-tertiary">
                Next →
              </div>
              <div class="mt-1 text-text-primary group-hover:text-accent-primary">
                {next.title}
              </div>
            </a>
          ) : <div />}
        </nav>
      )}
    </div>

    <aside class="hidden lg:block">
      <div class="sticky top-24">
        <PostToc headings={headings} />
      </div>
    </aside>
  </article>

  <BackToTop />
  <CodeCopyEnhancer />
</Base>
